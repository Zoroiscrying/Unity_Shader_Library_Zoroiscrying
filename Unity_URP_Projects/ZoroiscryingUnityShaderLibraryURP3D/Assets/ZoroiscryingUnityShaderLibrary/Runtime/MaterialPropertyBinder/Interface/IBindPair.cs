namespace ZoroiscryingUnityShaderLibrary.Runtime.MaterialBinder
{
    // The bind pair stores the variables needed to perform specific property binding operation.
    // 1. It will need the material property's name.
    // 2. It will need the bind option to select the right class that match the target material property
    public interface IBindPair
    {
        
    }
}